<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    style="@style/loginParent"
    android:background="@drawable/login"
    tools:context=".RegistrarseActivity">

    <ScrollView style="@style/parent">
        <RelativeLayout style="@style/parent">
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_alignParentTop="true"
                android:weightSum="12">

                <LinearLayout style="@style/parent"
                    android:background="@drawable/encabezado_form"
                    android:orientation="vertical"
                    android:layout_weight="3">

                    <ImageView style="@style/parent"
                        android:background="@drawable/encabezado_onda"
                        android:contentDescription="login_background"/>

                </LinearLayout>
            </LinearLayout>
            <LinearLayout
                android:layout_width="355dp"
                android:layout_height="wrap_content"
                android:layout_alignParentTop="true"
                android:layout_marginLeft="30dp"
                android:layout_marginTop="48dp"
                android:layout_marginRight="30dp"
                android:orientation="vertical">

                <ImageView style="@style/ilogin"
                    android:id="@+id/ilogin"
                    android:contentDescription="@string/ci"/>

                <include layout="@layout/layout_registrarse"/>
            </LinearLayout>
        </RelativeLayout>
    </ScrollView>
</RelativeLayout>